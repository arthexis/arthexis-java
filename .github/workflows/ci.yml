name: CI

on:
  pull_request:
  push:
    branches:
      - main
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch:

jobs:

  tests:
    name: tests
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven
      - name: Run test suite
        id: run_tests
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          set +e
          mvn -B test 2>&1 | tee ci-test.log
          status=${PIPESTATUS[0]}
          set -e
          echo "status=$status" >> "$GITHUB_OUTPUT"

          if [ "$status" -ne 0 ]; then
            {
              echo "error_message<<EOF"
              if grep -q "\[ERROR\]" ci-test.log; then
                grep "\[ERROR\]" ci-test.log | tail -n 40
              else
                tail -n 40 ci-test.log
              fi
              echo "EOF"
            } >> "$GITHUB_OUTPUT"
          fi
      - name: Comment on PR with test failure details
        if: github.event_name == 'pull_request' && steps.run_tests.outputs.status != '0'
        uses: actions/github-script@v7
        env:
          ERROR_MESSAGE: ${{ steps.run_tests.outputs.error_message }}
        with:
          script: |
            const marker = '<!-- arthexis-tests-failure-comment -->';
            const raw = process.env.ERROR_MESSAGE || 'No test error details captured.';
            const maxLength = 60000;
            const content = raw.length > maxLength
              ? `${raw.slice(0, maxLength)}\n... (truncated)`
              : raw;
            const prNumber = context.issue.number;
            const body = `${marker}
            ❌ CI **tests** job failed.

            - Workflow run: ${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}
            - Commit: ${context.sha}

            \`\`\`text
            ${content}
            \`\`\`
            `;

            const { owner, repo } = context.repo;
            const comments = await github.paginate(github.rest.issues.listComments, {
              owner,
              repo,
              issue_number: prNumber,
              per_page: 100
            });
            const existing = comments.find((comment) => comment.body && comment.body.includes(marker));

            if (existing) {
              await github.rest.issues.updateComment({
                owner,
                repo,
                comment_id: existing.id,
                body
              });
            } else {
              await github.rest.issues.createComment({
                owner,
                repo,
                issue_number: prNumber,
                body
              });
            }
      - name: Fail workflow if tests failed
        if: steps.run_tests.outputs.status != '0'
        shell: bash
        run: |
          echo "::error::Test suite failed. Showing the last captured error lines."
          if [ -f ci-test.log ]; then
            echo "----- [ERROR] lines from ci-test.log (last 60) -----"
            if grep -q "\[ERROR\]" ci-test.log; then
              grep "\[ERROR\]" ci-test.log | tail -n 60
            else
              tail -n 60 ci-test.log
            fi
          else
            echo "ci-test.log was not found."
          fi
          exit 1

  build:
    needs: tests
    if: |
      github.event_name == 'schedule' ||
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'pull_request' && needs.tests.result == 'failure')
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: write
    strategy:
      fail-fast: false
      matrix:
        install_mode: [new-install, upgrade-install]
    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven
      - name: Verify (${{ matrix.install_mode }})
        id: verify
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          set +e

          if [ "${{ matrix.install_mode }}" = "new-install" ]; then
            verify_command="mvn -B verify"
          else
            verify_command="mvn -B -Dtest=FlywayUpgradePathTests test"
          fi

          bash -lc "$verify_command" 2>&1 | tee ci-verify.log
          status=${PIPESTATUS[0]}
          set -e
          echo "status=$status" >> "$GITHUB_OUTPUT"

          if [ "$status" -ne 0 ]; then
            {
              echo "error_message<<EOF"
              if grep -q "\[ERROR\]" ci-verify.log; then
                grep "\[ERROR\]" ci-verify.log | tail -n 40
              else
                tail -n 40 ci-verify.log
              fi
              echo "EOF"
            } >> "$GITHUB_OUTPUT"
          fi
      - name: Open or update issue with failure context
        if: steps.verify.outputs.status != '0'
        uses: actions/github-script@v7
        env:
          ERROR_MESSAGE: ${{ steps.verify.outputs.error_message }}
        with:
          script: |
            const mode = '${{ matrix.install_mode }}';
            const title = `[CI] Hourly ${mode} validation failed`;
            const marker = `<!-- arthexis-hourly-ci-failure-${{ matrix.install_mode }} -->`;
            const raw = process.env.ERROR_MESSAGE || 'No error details captured.';
            const maxLength = 65000;
            const content = raw.length > maxLength
              ? `${raw.slice(0, maxLength)}\n... (truncated)`
              : raw;
            const body = `${marker}
            Hourly install/upgrade validation failed for **${mode}**.

            - Workflow run: ${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}
            - Commit: ${context.sha}

            \`\`\`text
            ${content}
            \`\`\`
            `;
            const { owner, repo } = context.repo;
            const issues = await github.paginate(github.rest.issues.listForRepo, {
              owner,
              repo,
              state: 'open',
              per_page: 100
            });
            const existing = issues.find((issue) => issue.title === title && issue.body && issue.body.includes(marker));
            if (existing) {
              await github.rest.issues.update({
                owner,
                repo,
                issue_number: existing.number,
                body
              });
            } else {
              await github.rest.issues.create({
                owner,
                repo,
                title,
                body,
                labels: ['ci']
              });
            }
      - name: Fail workflow if verify failed
        if: steps.verify.outputs.status != '0'
        run: |
          echo "Verification failed for install mode: ${{ matrix.install_mode }}"
          exit 1

  rpi-compat:
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    name: rpi-compat (${{ matrix.distro }} / ${{ matrix.install_mode }})
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - distro: debian-bookworm
            image: maven:3.9.9-eclipse-temurin-21
            install_mode: new-install
          - distro: ubuntu-noble
            image: maven:3.9.9-eclipse-temurin-21-noble
            install_mode: new-install
          - distro: debian-bookworm-upgrade
            image: maven:3.9.9-eclipse-temurin-21
            install_mode: upgrade-install
          - distro: ubuntu-noble-upgrade
            image: maven:3.9.9-eclipse-temurin-21-noble
            install_mode: upgrade-install
    steps:
      - uses: actions/checkout@v4
      - name: Set up QEMU for ARM64
        uses: docker/setup-qemu-action@v3
        with:
          platforms: arm64
      - name: Run rpi-compat tests (${{ matrix.distro }})
        id: verify_rpi
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          set +e

          if [ "${{ matrix.install_mode }}" = "upgrade-install" ]; then
            verify_command="mvn -B -Dtest=FlywayUpgradePathTests test"
          else
            verify_command="mvn -B -Dtest=ArthexisApplicationTests,ArchitectureTests,ModularityTests test"
          fi

          docker run --rm \
            --platform linux/arm64 \
            -v "$PWD":/workspace \
            -v "$HOME/.m2":/root/.m2 \
            -w /workspace \
            "${{ matrix.image }}" \
            bash -lc "$verify_command" 2>&1 | tee ci-rpi-verify.log
          status=${PIPESTATUS[0]}
          set -e
          echo "status=$status" >> "$GITHUB_OUTPUT"

          if [ "$status" -ne 0 ]; then
            {
              echo "error_message<<EOF"
              if grep -q "\[ERROR\]" ci-rpi-verify.log; then
                grep "\[ERROR\]" ci-rpi-verify.log | tail -n 40
              else
                tail -n 40 ci-rpi-verify.log
              fi
              echo "EOF"
            } >> "$GITHUB_OUTPUT"
          fi
      - name: Open or update issue with rpi-compat failure context
        if: steps.verify_rpi.outputs.status != '0'
        uses: actions/github-script@v7
        env:
          ERROR_MESSAGE: ${{ steps.verify_rpi.outputs.error_message }}
        with:
          script: |
            const distro = '${{ matrix.distro }}';
            const mode = '${{ matrix.install_mode }}';
            const title = `[CI] Hourly rpi-compat ${distro} ${mode} validation failed`;
            const marker = `<!-- arthexis-hourly-rpi-ci-failure-${{ matrix.distro }}-${{ matrix.install_mode }} -->`;
            const raw = process.env.ERROR_MESSAGE || 'No error details captured.';
            const maxLength = 65000;
            const content = raw.length > maxLength
              ? `${raw.slice(0, maxLength)}\n... (truncated)`
              : raw;
            const body = `${marker}
            Hourly rpi-compat validation failed for **${distro} / ${mode}**.

            - Workflow run: ${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}
            - Commit: ${context.sha}

            \`\`\`text
            ${content}
            \`\`\`
            `;
            const { owner, repo } = context.repo;
            const issues = await github.paginate(github.rest.issues.listForRepo, {
              owner,
              repo,
              state: 'open',
              per_page: 100
            });
            const existing = issues.find((issue) => issue.title === title && issue.body && issue.body.includes(marker));
            if (existing) {
              await github.rest.issues.update({
                owner,
                repo,
                issue_number: existing.number,
                body
              });
            } else {
              await github.rest.issues.create({
                owner,
                repo,
                title,
                body,
                labels: ['ci']
              });
            }
      - name: Fail workflow if rpi-compat verify failed
        if: steps.verify_rpi.outputs.status != '0'
        run: |
          echo "RPI compatibility verification failed for: ${{ matrix.distro }} / ${{ matrix.install_mode }}"
          exit 1

  ui-screenshots:
    name: ui-screenshots
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Install Chromium for Playwright
        run: npx --yes -p playwright@1.53.0 playwright install --with-deps chromium
      - name: Capture admin and user UI screenshots
        id: capture_screenshots
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          set +e
          mkdir -p artifacts/screenshots
          cat > ci-capture-screenshots.js <<'EOF_NODE'
          const { chromium } = require('playwright');
          const path = require('node:path');

          (async () => {
            const browser = await chromium.launch();
            const page = await browser.newPage({ viewport: { width: 1440, height: 900 } });

            const captures = [
              {
                input: path.resolve('src/main/resources/static/admin/index.html'),
                output: path.resolve('artifacts/screenshots/admin-ui.png')
              },
              {
                input: path.resolve('src/main/resources/static/cp/index.html'),
                output: path.resolve('artifacts/screenshots/user-ui.png')
              }
            ];

            for (const capture of captures) {
              await page.goto(`file://${capture.input}`);
              await page.screenshot({ path: capture.output, fullPage: true });
            }

            await browser.close();
          })();
          EOF_NODE
          npx --yes -p playwright@1.53.0 -c 'node ci-capture-screenshots.js' 2>&1 | tee ci-ui-screenshots.log
          status=${PIPESTATUS[0]}
          rm -f ci-capture-screenshots.js
          set -e
          echo "status=$status" >> "$GITHUB_OUTPUT"

          if [ "$status" -ne 0 ]; then
            {
              echo "error_message<<EOF"
              tail -n 80 ci-ui-screenshots.log
              echo "EOF"
            } >> "$GITHUB_OUTPUT"
          fi
      - name: Upload UI screenshots artifact
        uses: actions/upload-artifact@v4
        with:
          name: ui-screenshots
          path: artifacts/screenshots
      - name: Comment on PR with screenshot failure details
        if: github.event_name == 'pull_request' && steps.capture_screenshots.outputs.status != '0'
        uses: actions/github-script@v7
        env:
          ERROR_MESSAGE: ${{ steps.capture_screenshots.outputs.error_message }}
        with:
          script: |
            const marker = '<!-- arthexis-ui-screenshots-failure-comment -->';
            const raw = process.env.ERROR_MESSAGE || 'No screenshot error details captured.';
            const maxLength = 60000;
            const content = raw.length > maxLength
              ? `${raw.slice(0, maxLength)}\n... (truncated)`
              : raw;
            const prNumber = context.issue.number;
            const body = `${marker}
            ❌ CI **ui-screenshots** job failed.

            - Workflow run: ${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}
            - Commit: ${context.sha}

            \`\`\`text
            ${content}
            \`\`\`
            `;

            const { owner, repo } = context.repo;
            const comments = await github.paginate(github.rest.issues.listComments, {
              owner,
              repo,
              issue_number: prNumber,
              per_page: 100
            });
            const existing = comments.find((comment) => comment.body && comment.body.includes(marker));

            if (existing) {
              await github.rest.issues.updateComment({
                owner,
                repo,
                comment_id: existing.id,
                body
              });
            } else {
              await github.rest.issues.createComment({
                owner,
                repo,
                issue_number: prNumber,
                body
              });
            }
      - name: Fail workflow if screenshot capture failed
        if: steps.capture_screenshots.outputs.status != '0'
        shell: bash
        run: |
          echo "::error::UI screenshot capture failed. Showing captured output."
          if [ -f ci-ui-screenshots.log ]; then
            tail -n 120 ci-ui-screenshots.log
          else
            echo "ci-ui-screenshots.log was not found."
          fi
          exit 1
